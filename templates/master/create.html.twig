{% extends 'base.html.twig' %}

{% block title %}Create new Status
{% endblock %}

{% block body %}
	<div class="container">
		<div class="row justify-content-center">
			<div class="col-md-8">
				<div class="card">
					<div class="card-header">
						<h1>Create new Status</h1>
					</div>
					<div class="card-body">
						<form action="{{path('master_store')}}" method="POST">
							<div class="form-group">
								<label>Name:</label>
								<input type="text" class="form-control" name="master_name" value="{{master_name}}">
							</div>
							<button type="submit" class="btn btn-primary">Add new Status</button>
							<input type="hidden" name="token" value="{{ csrf_token('create_master_bla') }}"/>
						</form>
					</div>
				</div>
			</div>
		</div>
	</div>
</div>{% endblock %}
