{% extends 'base.html.twig' %}

{% block title %}Tasks list
{% endblock %}

{% block body %}

	<div class="container">
		<div class="row justify-content-center">
			<div class="col-md-8">
				<div class="card">
					<div class="card-header">
						<h1>Tasks list</h1>
					</div>
					<div class="card-body">
						List order by:

						<form action="{{ path('outfit_index')}}" method="GET" class="sort-form	">
							<div class="form-group">
								<select name="master_id" class="form-control">
									<option value="all">View All</option>
									{% for master in masters %}
										<option value="{{master.id}}" {% if masterId == master.id %} selected {% endif %}>{{master.name}}</option>
									{% endfor %}
								</select>
							</div>
							<button type="submit" class="btn btn-dark">Filter</button>
						</form>

						<ul class="list-group">
							{% for outfit in outfits %}
								<li class="list-group-item">
									<div class="list-group-item__line">
										<span>
											{{outfit.type}} {{outfit.size}}
										</span>
										<div class="list-group-item__line__buttons">
											<a href="{{ path('outfit_edit', {id: outfit.id}) }}" class="btn btn-success">EDIT</a>
											<form action="{{ path('outfit_delete', {id: outfit.id}) }}" method="POST">
												<button type="submit" class="btn btn-danger">DELETE</button>
											</form>
										</div>
									</div>
									<h4>
										<span class="badge badge-secondary">Status</span>
									</h4>
									<ul class="list-group">

									
											<li class="list-group-item">{{outfit.master.name}}</li>
									
									</ul>
								</li>
							{% endfor %}
						</ul>
					</div>
				</div>
			</div>
		</div>
	</div>


{% endblock %}
